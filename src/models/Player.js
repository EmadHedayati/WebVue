import Account from "./Account";

class Player extends Account {
    get _attributes(){
        return {
        }
    }

    constructor(attributes){
        super(attributes);
    }
}

export default Player;
